# agent_mash/tests/fixtures/__init__.py
"""
Централизованный пакет фикстур для тестового контура agent_mash.

Назначение:
- Явная регистрация всех pytest-фикстур проекта
- Контроль экспортируемого API фикстур
- Предсказуемая работа pytest discovery
- Безопасная инициализация без побочных эффектов

Данный файл намеренно не содержит логики исполнения.
Он используется исключительно как агрегатор фикстурных модулей.

Документация pytest:
https://docs.pytest.org/en/stable/explanation/fixtures.html
"""

from __future__ import annotations

# Импортируй здесь ВСЕ модули фикстур проекта.
# Это гарантирует их регистрацию pytest на этапе discovery.
#
# Пример (раскомментировать при добавлении файлов):
# from .db import *
# from .api import *
# from .auth import *
# from .config import *

__all__: list[str] = [
    # Явно перечисляй публичные фикстуры,
    # если используешь selective import в тестах.
    #
    # Пример:
    # "db_session",
    # "api_client",
    # "auth_token",
]

# Защита от случайного исполнения при запуске как скрипт
# (не обязательна, но допустима в промышленном стандарте).
if __name__ == "__main__":
    raise RuntimeError(
        "Пакет fixtures не предназначен для прямого запуска"
    )
