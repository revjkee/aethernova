agent_mash/legacy/scripts_archive/utilities/legacy_env_loader.py: безопасная загрузка .env/env-файлов с приоритетами, профилями, валидацией, маскированием секретов, CLI, без внешних зависимостей.

Ключевой вопрос
Нужен готовый промышленный код для legacy_env_loader.py, который надёжно загружает переменные окружения из файлов и применяет их к процессу.

Ключевые компоненты
Файлы окружения (например .env, .env.local, .env.prod), порядок приоритетов, безопасный парсинг, контроль обязательных ключей, логирование без утечек секретов, CLI.

Логические связи
Профиль выбирает набор файлов, дальше применяется детерминированный порядок наложения; конфликты решаются правилом приоритета; секреты не печатаются; при необходимости выполняется валидация обязательных ключей.

Синтез
Скрипт предоставляет API (для импорта) и CLI (для вызова), единые правила приоритетов, строгий парсер, и безопасный вывод.

Вывод
Самый полезный ответ: готовый файл кода, который можно сразу положить в указанный путь и использовать в существующей системе.