agent_mash/legacy/scripts_archive/auth/deprecated_login_flow.py. Это архивный, безопасно-ограниченный скрипт (по умолчанию не выполняет сетевые вызовы) для воспроизведения устаревшего login-flow в тестовых контурах. Реальный контракт вашего API я не могу подтвердить без спецификации.

ANSWERING EXAMPLE

Step-by-step answer with CONCRETE details and key context.

Ключевой вопрос
Нужен промышленный архивный скрипт для устаревшего login flow, который не создаёт рисков (случайные запросы, утечки секретов), но пригоден для диагностики и воспроизводимости.

Ключевые компоненты
Защитный флаг запуска, строгая валидация входа, таймауты, редактирование секретов в логах, детерминированный вывод.

Логические связи
Архивный код должен быть безопасным по умолчанию и пригодным для аудита. Сетевые запросы допускаются только при явном подтверждении.