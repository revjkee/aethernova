# Archived Observability Core infrastructure

These files are retained for historical reference only. They were removed from
the active core because they referenced missing certificates, directories,
Prometheus rules, or custom functions and duplicated services already owned by
the repository root.

Active ownership now lives in:

- `docker-compose.yml`
- `monitoring/prometheus/prometheus.yml`
- `monitoring/prometheus/rules/`
- `core-systems/observability-core/src/observability_core/`
