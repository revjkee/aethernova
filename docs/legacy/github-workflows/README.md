# Archived GitHub workflows

These files are historical and are not executed by GitHub Actions.

They were moved out of `.github/workflows/` because they referenced missing
deployment infrastructure, unavailable self-hosted runners, placeholder
domains, stale product names, or test paths that do not exist. They must not be
reactivated without an owner, documented secrets, a rollback procedure, and a
successful dry run.
