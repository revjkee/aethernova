# airdrop_manager/.gitignore
# -----------------------------------------------------------------------------
# 0) CRITICAL SECRETS & PRIVATE KEYS (do not commit)
# -----------------------------------------------------------------------------
# Common secrets
*.env
.env
.env.*
.env.local
.env.*.local
*.secret
*.secrets
*.vault
*.keystore
*.p12
*.pfx
*.pem
*.key
*.crt
*.cer
*.der
*.asc
*.gpg
*.age
id_rsa
id_dsa
id_ecdsa
id_ed25519
ssh_host_*
*.kdbx

# Allow safe templates/examples
!*.example
!*.sample
!*.dist
!.env.example
!.env.sample
!.env.dist

# Local credentials/configs
*.tfvars
*.tfvars.json
*.override.tfvars
*.override.tfvars.json

# -----------------------------------------------------------------------------
# 1) LOGS, RUNTIME & DUMPS
# -----------------------------------------------------------------------------
*.log
*.log.*
logs/
log/
*.pid
*.seed
*.pid.lock
*.out
*.err
*.trace
*.stackdump
*.dmp
dump/
dumps/

# -----------------------------------------------------------------------------
# 2) PYTHON (build, caches, venv)
# -----------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.coverage
.coverage.*
.cache/
.mypy_cache/
.ruff_cache/
.pytype/
.ipynb_checkpoints/
pip-wheel-metadata/
.tox/
.nox/
dist/
build/
develop-eggs/
.eggs/
*.egg-info/
*.egg
wheels/
venv/
.venv/
env/
ENV/
.poetry-cache/

# -----------------------------------------------------------------------------
# 3) NODE / FRONTEND (npm/yarn/pnpm, bundlers)
# -----------------------------------------------------------------------------
node_modules/
.npm/
.yarn/
.yarn/cache/
.pnpm-store/
.pnp/
.pnp.js
.pnp.cjs
.babel-cache/
.parcel-cache/
.npmrc
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
# Bundlers & frameworks
dist/
build/
.cache-loader/
.next/
.nuxt/
out/
.storybook-out/
coverage/
coverage-final.json
.lighthouseci/

# Keep lockfiles for reproducible builds
!package-lock.json
!yarn.lock
!pnpm-lock.yaml

# -----------------------------------------------------------------------------
# 4) WEB3 / SOLIDITY (Hardhat, Foundry, Truffle, Brownie)
# -----------------------------------------------------------------------------
# Hardhat
artifacts/
cache/
cache-*
typechain/
typechain-types/

# Foundry
out/
broadcast/
cache/

# Truffle
build/contracts/

# Brownie
build/
reports/

# OpenZeppelin upgrades (local manifest)
.openzeppelin/

# -----------------------------------------------------------------------------
# 5) GO / RUST / JAVA (if used)
# -----------------------------------------------------------------------------
# Go
bin/
vendor/
# Rust
target/
# Keep Cargo.lock (recommended for bins)
!Cargo.lock
# Java/Gradle/Maven
target/
.gradle/
.build/
*.class
*.jar
*.war
*.ear

# -----------------------------------------------------------------------------
# 6) DOCKER / CONTAINERS
# -----------------------------------------------------------------------------
docker-data/
docker-volumes/
*.tar
*.img
*.qcow2
.docker/
docker-compose.override.yml

# -----------------------------------------------------------------------------
# 7) TERRAFORM / HELM / K8S
# -----------------------------------------------------------------------------
.terraform/
.terraform.*
terraform.tfstate
terraform.tfstate.*
crash.log
crash.*.log
*.tfplan
override.tf
override.tf.json
*_override.tf
*_override.tf.json
# Keep dependency lock for providers
!.terraform.lock.hcl

# Helm
charts/*/charts/
charts/*/tmp/
*.tgz

# Kubernetes local config (avoid leaking kubeconfigs)
.kube/
kubeconfig
*.kubeconfig

# -----------------------------------------------------------------------------
# 8) TEST ARTIFACTS & REPORTS
# -----------------------------------------------------------------------------
junit*.xml
pytest.xml
test-results/
test-output/
htmlcov/
reports/
.allure/
allure-results/
allure-report/

# -----------------------------------------------------------------------------
# 9) EDITORS & IDEs
# -----------------------------------------------------------------------------
# VS Code
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/launch.json
!.vscode/tasks.json

# JetBrains
.idea/
*.iml

# Visual Studio
.vs/

# Others
*.swp
*.swo
*~
.vim/
.history/

# -----------------------------------------------------------------------------
# 10) OS-SPECIFIC NOISE
# -----------------------------------------------------------------------------
# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# Linux
*~
.fuse_hidden*
.directory
.nfs*

# -----------------------------------------------------------------------------
# 11) BINARIES & OBJECTS
# -----------------------------------------------------------------------------
*.o
*.obj
*.so
*.dylib
*.dll
*.exe
*.a
*.lib
*.pdb
*.nupkg

# -----------------------------------------------------------------------------
# 12) DATA & TEMP (local dev-only)
# -----------------------------------------------------------------------------
tmp/
temp/
tmp-*/
data/
data-*/
*.sqlite
*.sqlite3
*.db
*.db-*
*.rdb
*.dump
*.bak
*.bak-*
uploads/
.cache/
