# NOTE: Этот файл-заглушка включён для корректной структуры репозитория.
# I cannot verify this.
# Для детерминированной сборки lock должен быть сгенерирован в целевой среде.
# Его наличие как пустого файла допустимо до первого `poetry lock`.

[[metadata.files]]
# intentionally empty

[metadata]
lock-version = "2.0"
python-versions = ">=3.11,<3.13"
content-hash = "<to-be-generated-by-poetry>"
