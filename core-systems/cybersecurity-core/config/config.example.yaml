# Конфигурация для cybersecurity-core

system:
  name: "cybersecurity-core"
  version: "1.0.0"
  debug: false

logging:
  level: "INFO"
  format: "{time} | {level} | {message}"
  file: "logs/cybersecurity-core.log"

integration:
  enabled: true
  core_systems_path: "/workspaces/aethernova/core-systems"
  
security:
  enabled: true
  encryption_key: null

# Специфичные настройки для cybersecurity-core
# TODO: Добавить настройки специфичные для данной системы
