# =============================================================================
# NeuroForge Core — Industrial .gitignore
# =============================================================================
# ВНИМАНИЕ: .gitignore — это защита "лучшее усилие". Никогда не храните реальные
# секреты в репозитории. Используйте секрет-менеджеры и CI-хранилища секретов.
# =============================================================================

# ---------------------------
# БАЗОВОЕ: Git артефакты
# ---------------------------
*.orig
*.rej
*.patch
*.diff
*.backup
*.bak
*.tmp
*.swp
*.swo
*.sw?
*~
!.gitignore
!.gitattributes

# ---------------------------
# ОС-специфика
# ---------------------------
# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk
# Linux / KDE / misc
.directory

# ---------------------------
# IDE / Редакторы
# ---------------------------
# VS Code
.vscode/
!.vscode/extensions.json
!.vscode/settings.json
# JetBrains IDEs
.idea/
# Others
*.iml

# ---------------------------
# Python
# ---------------------------
__pycache__/
*.py[cod]
*$py.class
# Пакетирование/сборка
build/
dist/
wheels/
*.egg-info/
.eggs/
# Venv
.venv/
venv/
env/
ENV/
# Инструменты/кеши
.pytest_cache/
.coverage
.coverage.*
.cache/
.tox/
.nox/
.mypy_cache/
.pytype/
.ruff_cache/
.hypothesis/
.dmypy.json
.ipynb_checkpoints/
.jupyter/
# Зависимости-менеджеры
pip-wheel-metadata/
pip-log.txt
# Poetry / Pipenv — lock-файлы храним, каталоги кэшей — нет
.poetry-cache/
.pipenv/
# Cython/Numba
*.pyd
*.pyo
*.pkl

# ---------------------------
# Node / TypeScript (если присутствует фронтенд/скрипты)
# ---------------------------
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.parcel-cache/
.eslintcache
.next/
out/

# ---------------------------
# Логи / метрики / профилировка
# ---------------------------
logs/
*.log
log.txt
profile.out
perf.data
htmlcov/
coverage.xml
cov.xml
pytestdebug.log

# ---------------------------
# Docker / Compose / Dev контейнеры
# ---------------------------
docker/*.local.*
docker/.tmp/
.docker/
docker-compose.override.yml
docker-compose.*.local.yml
# Локальные окружения
.env
.env.*
!.env.example
!.env.*.example
.envrc

# ---------------------------
# Kubernetes / Helm
# ---------------------------
.kube/
kubeconfig
helm-chart-*.tgz
charts/*.tgz
# Потенциально секретные манифесты
*secret*.yml
*secret*.yaml
!*secret*.example.yml
!*secret*.example.yaml

# ---------------------------
# Terraform
# ---------------------------
.terraform/
terraform.tfstate
terraform.tfstate.*
crash.log
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraform.lock.hcl

# ---------------------------
# CI/CD временные артефакты
# ---------------------------
ci_tmp/
.artifacts/
reports/
test-results/

# ---------------------------
# Данные / ML / Научные артефакты
# ---------------------------
# Каталоги данных (оставьте README или .keep внутри, если хотите зафиксировать структуру)
data/
datasets/
raw/
processed/
external/
# Разрешить плейсхолдеры
!data/.gitkeep
!datasets/.gitkeep
!raw/.gitkeep
!processed/.gitkeep
!external/.gitkeep

# Чекпоинты/веса/модели
checkpoints/
models/
weights/
artifacts/
experiments/
outputs/
runs/
lightning_logs/
wandb/
mlruns/
tensorboard/
tb_logs/
# Файлы весов/моделей
*.pt
*.pth
*.ckpt
*.onnx
*.tflite
*.h5
*.pb
*.sav
*.joblib
*.npz
*.npy
*.pkl
*.bin
*.safetensors

# Большие файлы/форматы данных (не для отслеживания по умолчанию)
*.parquet
*.arrow
*.feather
*.orc
*.gz
*.bz2
*.xz
*.zip
*.7z
*.tar
*.tar.gz
*.tar.zst
*.zst

# ---------------------------
# Конфигурации (локальные/секретные)
# ---------------------------
# Общие локальные конфиги
*.local.yml
*.local.yaml
*.local.json
*.dev.yml
*.dev.yaml
*.override.yml
*.override.yaml

# Секреты и ключи (Шаблоны разрешаем)
secrets/
private/
*.pem
*.key
*.der
*.p12
*.pfx
*.crt
*.csr
ssh_known_hosts
id_rsa
id_dsa
id_ecdsa
id_ed25519
known_hosts
!secrets/.gitkeep
!private/.gitkeep
# Образцы конфигов/секретов
!*.example
!*.example.yml
!*.example.yaml
!*.example.json
!secrets/*.example.yml
!secrets/*.example.yaml

# ---------------------------
# Документация (кеши генераторов)
# ---------------------------
site/
docs/_build/
docs/.cache/
.mkdocs-cache/
.docusaurus/
# Разрешить исходники
!docs/source/**

# ---------------------------
# Артефакты контейнеризации/пайплайнов (локальные)
# ---------------------------
volumes/
tmp/
tmp_*/
temp/
.cache_dir/
.cache_db/

# ---------------------------
# Пакеты/публикации (если используются локально)
# ---------------------------
*.whl
*.tar.bz2
*.tar.xz

# ---------------------------
# Разрешённые плейсхолдеры
# ---------------------------
!.gitkeep
!.keep
README.md
