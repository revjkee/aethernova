# neuroforge-core/poetry.lock
# Placeholder lockfile: структура валидна, данные зависимостей не заявлены.
# Внимание: этот файл намеренно не содержит секций [[package]].
# После появления реального pyproject.toml запустите:
#   poetry lock --no-interaction
# чтобы сгенерировать полноценный lock с разрешенными версиями и файлами.

[metadata]
lock-version = "2.0"
python-versions = "*"
# Хэш содержимого pyproject.toml, используемый Poetry для валидации lock.
# Здесь метка-заглушка, так как pyproject.toml не предоставлен.
content-hash = "UNVERIFIED"

# Секция с файлами артефактов (sdist/wheel) для пакетов.
# Пустая, т.к. пакеты не фиксируются в заглушке.
[metadata.files]
