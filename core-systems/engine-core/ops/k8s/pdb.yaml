apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: engine-core-pdb
  namespace: backend
  labels:
    app: engine-core
spec:
  selector:
    matchLabels:
      app: engine-core
  maxUnavailable: 1
  # Альтернатива:
  # minAvailable: 2
