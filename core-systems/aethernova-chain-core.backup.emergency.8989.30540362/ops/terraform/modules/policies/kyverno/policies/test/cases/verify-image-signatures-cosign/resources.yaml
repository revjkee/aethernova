apiVersion: v1
kind: Pod
metadata:
  name: verify-image-cosign-signed-ok
  namespace: supplychain-dev
  labels:
    app.kubernetes.io/name: verify-image-cosign-signed-ok
    app.kubernetes.io/part-of: aethernova-chain-core
    app.kubernetes.io/managed-by: kyverno-test
spec:
  restartPolicy: Never
  containers:
    - name: app
      # Подписанный демонстрационный образ из официальной документации Kyverno
      # Используется для успешного прохождения правила verifyImages (Cosign)
      image: ghcr.io/kyverno/test-verify-image:signed
      command: ["sh", "-c", "sleep 300"]
