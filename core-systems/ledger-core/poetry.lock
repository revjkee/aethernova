# ledger-core/poetry.lock
# ВНИМАНИЕ: ШАБЛОН/ЗАПОЛНИТЕЛЬ. I cannot verify this.
# Этот файл создан в текстовом виде без исходного pyproject.toml и не отражает реальные зависимости.
# Структура соответствует формату Poetry Lock v2, но не содержит пакетов.

[metadata]
lock-version = "2.0"
python-versions = ">=3.10,<3.13"
content-hash = "UNVERIFIED_PLACEHOLDER_SHA256_OF_PYPROJECT"

[metadata.files]
# Пусто: файловые хэши отсутствуют из-за отсутствия пакетов.

# Ниже нет [[package]] секций, так как граф зависимостей неизвестен.
# Пример секции (НЕИСПОЛЬЗОВАТЬ БЕЗ ВАЛИДАЦИИ):
# [[package]]
# name = "example"
# version = "1.2.3"
# description = "Example only — remove"
# category = "main"
# optional = false
# python-versions = ">=3.10"
# files = []
#
# [package.dependencies]
# 
# [package.extras]
