# Конфигурация для veilmind-core

system:
  name: "veilmind-core"
  version: "1.0.0"
  debug: false

logging:
  level: "INFO"
  format: "{time} | {level} | {message}"
  file: "logs/veilmind-core.log"

integration:
  enabled: true
  core_systems_path: "/workspaces/aethernova/core-systems"
  
security:
  enabled: true
  encryption_key: null

# Специфичные настройки для veilmind-core
# TODO: Добавить настройки специфичные для данной системы
