# =============================================================================
# zero-trust-core /.gitignore — Industrial Baseline
# Политика: не хранить секреты и бинарные артефакты; хранить lock-файлы для
# воспроизводимости. Разрешены только зашифрованные/индиректные секреты (SOPS,
# SealedSecrets, ExternalSecrets, CSI SecretProviderClass).
# =============================================================================

# --- OS junk -----------------------------------------------------------------
.DS_Store
.AppleDouble
.LSOverride
Icon?
Thumbs.db
ehthumbs.db
Desktop.ini

# --- Editors / IDE -----------------------------------------------------------
.vscode/
.idea/
*.iml
*.ipr
*.iws
.vscode-test/
.vim/
*~
*.swp
*.swo
*.swn
.emacs.d/
.spyproject/
.cache/
*.orig
*.rej

# --- Python ------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
.python-version
pip-wheel-metadata/
.eggs/
*.egg-info/
dist/
build/
.wheelhouse/
.tox/
.nox/
.pytest_cache/
.pyre/
.mypy_cache/
.pytype/
coverage.xml
.coverage
.coverage.*
htmlcov/
site/

# --- Node / Frontend ---------------------------------------------------------
node_modules/
.pnpm-store/
.npm/
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.bun/
dist/
build/
.vite/
.next/
out/
.nyc_output/
.eslintcache

# Локальные env-файлы (оставьте шаблоны)
.env
.env.*
!.env.example
!.env.sample
!.env.dist

# --- Go ----------------------------------------------------------------------
bin/
cmd/**/bin/
*.test
*.out
coverage.txt
# lock-файлы Go должны быть в VCS:
!go.mod
!go.sum

# --- Java / JVM --------------------------------------------------------------
target/
.gradle/
build/
out/
.settings/
.classpath
.project
*.class
*.jar
*.war
*.ear

# --- Rust --------------------------------------------------------------------
target/
**/*.rs.bk
# lock-файл Rust должен быть в VCS:
!Cargo.lock

# --- Docker / Containers -----------------------------------------------------
.docker/
docker/*.env
docker-compose.override.yml
docker-compose.local.yml
# .dockerignore обычно хранится в VCS (не игнорируем)

# --- Terraform ---------------------------------------------------------------
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraform.tfvars
terraform.tfvars
*.auto.tfvars
# lock-файл Terraform должен быть в VCS:
!.terraform.lock.hcl

# --- Helm --------------------------------------------------------------------
charts/*.tgz
*.chart.lock
Chart.lock
helmfile.d/helmfile*.lock
# values с секретами не храним
values.secret.yaml
values-*.secret.yaml

# --- Kubernetes (манивесты/ключи) -------------------------------------------
kubeconfig
kubeconfig.*
*.kubeconfig
*.crt
*.key
*.pem
*.p12
*.pfx
*.keystore

# Откровенные секреты не коммитим
secret-*.yaml
secrets/*.yaml

# Разрешаем GitOps-безопасные форматы секретов
!**/*sealed*.yaml              # Bitnami SealedSecret
!**/*.sops.yaml                # SOPS encrypted files
!**/*ExternalSecret*.yaml      # External Secrets Operator
!**/SecretProviderClass*.yaml  # CSI Secrets Store

# --- Ansible -----------------------------------------------------------------
*.retry
*.vault_pass
.vault_pass.txt
ansible.log
inventory/*.ini
inventory/*.yml
!inventory/group_vars/
!inventory/host_vars/

# --- Logs / Temp / Reports ---------------------------------------------------
*.log
logs/
log/
tmp/
temp/
*.tmp
*.bak
*.old
.trash/
reports/
test-results/
junit.xml

# --- Archives / Artifacts ----------------------------------------------------
*.zip
*.tar
*.tar.gz
*.tgz
*.7z
*.rar

# --- Security & scanners outputs --------------------------------------------
trivy-report.*
kube-bench-*.json
kube-bench-*.txt
sarif-results/
dependency-check-report.*

# --- Misc --------------------------------------------------------------------
*.pid
*.lock
!.keep        # разрешить маркеры пустых директорий

# =============================================================================
# Конец файла
# =============================================================================
