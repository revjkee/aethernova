[tool.poetry]
name = "liquidity-core"
version = "0.1.0"
description = "Microservice for liquidity-core"
authors = ["Admin <admin@example.com>"]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
