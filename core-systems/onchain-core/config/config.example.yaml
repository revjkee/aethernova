# Конфигурация для onchain-core

system:
  name: "onchain-core"
  version: "1.0.0"
  debug: false

logging:
  level: "INFO"
  format: "{time} | {level} | {message}"
  file: "logs/onchain-core.log"

integration:
  enabled: true
  core_systems_path: "/workspaces/aethernova/core-systems"
  
security:
  enabled: true
  encryption_key: null

# Специфичные настройки для onchain-core
# TODO: Добавить настройки специфичные для данной системы
