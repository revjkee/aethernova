---
- name: Apply common role with high-priority variables
  hosts: all
  become: true
  vars:
    # Пример переменных с более высоким приоритетом для роли common
    some_variable: "high_priority_value"
    another_variable: 42

  roles:
    - role: common
