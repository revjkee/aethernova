# Конфигурация для platform_ops-core

system:
  name: "platform_ops-core"
  version: "1.0.0"
  debug: false

logging:
  level: "INFO"
  format: "{time} | {level} | {message}"
  file: "logs/platform_ops-core.log"

integration:
  enabled: true
  core_systems_path: "/workspaces/aethernova/core-systems"
  
security:
  enabled: true
  encryption_key: null

# Специфичные настройки для platform_ops-core
# TODO: Добавить настройки специфичные для данной системы
