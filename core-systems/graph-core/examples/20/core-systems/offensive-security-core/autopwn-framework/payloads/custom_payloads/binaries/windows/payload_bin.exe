# payload_bin.exe — бинарник Windows (PE формат).
# Его нельзя передать как текст, так как это исполняемый файл.
# В реальном проекте payload_bin.exe скомпилирован из исходного кода (C/C++ или другого).
# Используется loader.py для запуска этого payload на целевой Windows машине.
# Важно: payload должен быть подготовлен и протестирован на безопасность и корректность.
