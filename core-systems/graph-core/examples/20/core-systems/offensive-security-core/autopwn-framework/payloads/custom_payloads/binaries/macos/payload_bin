# payload_bin — бинарник macOS (Mach-O формат).
# Его нельзя передать как текст, так как это исполняемый файл.
# В реальном проекте payload_bin скомпилирован из исходного кода (C/C++ или другого).
# Используется loader.py для запуска этого payload на целевой macOS машине.
# Важно: payload должен быть подготовлен и протестирован на безопасность и корректность.
