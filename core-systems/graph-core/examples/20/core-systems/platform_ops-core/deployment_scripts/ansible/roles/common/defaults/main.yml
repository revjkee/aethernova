---
# Значения переменных по умолчанию для роли common

timezone: "UTC"
ntp_servers:
  - "0.pool.ntp.org"
  - "1.pool.ntp.org"

ssh_port: 22
ssh_permit_root_login: "no"
ssh_password_authentication: "no"

users:
  - name: "deploy"
    groups: "sudo"
    shell: "/bin/bash"
    ssh_key: ""

firewall_enabled: true

packages_common:
  - "vim"
  - "curl"
  - "wget"
  - "git"
  - "ufw"

